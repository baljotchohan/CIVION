"use client";

import React, { useState } from 'react';
import { PredictionCard, Prediction } from '@/components/predictions/PredictionCard';
import { EmptyState } from '@/components/ui/EmptyState';

// Dummy data for predictions since it's not fully wired in SystemStateContext yet
const dummyPredictions: Prediction[] = [
    {
        id: 'pred_1',
        title: 'Major AI model release expected in Q3 targeting agentic workflows',
        probability: 0.85,
        timeframe: 'Next 30 Days',
        consensus_level: 'high',
        status: 'active',
        evidence: [
            'Multiple GitHub commits in secondary repos hinting at API changes',
            'Sudden increase in arxiv papers regarding structured output generation from key authors',
            'Signals from 3 developer forums discussing closed beta access'
        ]
    },
    {
        id: 'pred_2',
        title: 'Market correction in AI-related hardware stocks',
        probability: 0.62,
        timeframe: '1-2 Weeks',
        consensus_level: 'medium',
        status: 'active',
        evidence: [
            'Supply chain analyst reports indicating temporary component shortage',
            'Historical sentiment analysis on X showing "bubble" discourse increasing 40%',
            'Options market data showing increased put volume'
        ]
    },
    {
        id: 'pred_3',
        title: 'New cryptography standard adoption by major browser',
        probability: 0.95,
        timeframe: 'Resolved',
        consensus_level: 'high',
        status: 'resolved_correct',
        evidence: [
            'Chromium Gerrit commit merged',
            'RFC finalized yesterday',
            'Statement from working group'
        ]
    }
];

export default function PredictionsPage() {
    const [filter, setFilter] = useState<'all' | 'active' | 'resolved'>('active');

    // In a real app, you would fetch these from an API
    const predictions = dummyPredictions;

    const filtered = predictions.filter(p => {
        if (filter === 'all') return true;
        if (filter === 'active') return p.status === 'active';
        if (filter === 'resolved') return p.status.startsWith('resolved');
        return true;
    });

    return (
        <div className="space-y-6 animate-in slide-in-from-bottom-4 fade-in duration-500 max-w-5xl mx-auto">

            <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                <div>
                    <h1 className="text-2xl font-bold text-text-primary tracking-tight">Predictions Ledger</h1>
                    <p className="text-sm text-text-secondary mt-1">Synthesized probabilistic forecasts generated by the network.</p>
                </div>

                <div className="flex p-1 bg-bg-subtle border border-border rounded-lg">
                    <button
                        onClick={() => setFilter('active')}
                        className={`px-3 py-1.5 text-sm font-medium rounded-md transition-all ${filter === 'active' ? 'bg-bg-card shadow-sm text-text-primary' : 'text-text-muted hover:text-text-primary'}`}
                    >
                        Active
                    </button>
                    <button
                        onClick={() => setFilter('resolved')}
                        className={`px-3 py-1.5 text-sm font-medium rounded-md transition-all ${filter === 'resolved' ? 'bg-bg-card shadow-sm text-text-primary' : 'text-text-muted hover:text-text-primary'}`}
                    >
                        Resolved
                    </button>
                    <button
                        onClick={() => setFilter('all')}
                        className={`px-3 py-1.5 text-sm font-medium rounded-md transition-all ${filter === 'all' ? 'bg-bg-card shadow-sm text-text-primary' : 'text-text-muted hover:text-text-primary'}`}
                    >
                        All
                    </button>
                </div>
            </div>

            {filtered.length === 0 ? (
                <EmptyState
                    title="No Predictions Found"
                    description="The network hasn't generated predictions matching your filter criteria yet."
                    className="mt-12"
                />
            ) : (
                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                    {filtered.map(pred => (
                        <PredictionCard key={pred.id} prediction={pred} />
                    ))}
                </div>
            )}

        </div>
    );
}
