{
  "name": "civion",
  "version": "2.0.0",
  "description": "AI Workforce Platform - The Spider Network",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "build": "npm -w frontend run build",
    "start": "npm -w backend start",
    "test": "npm -w backend run test && npm -w frontend run test",
    "backend": "npm -w backend run dev",
    "frontend": "npm -w frontend run dev",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
